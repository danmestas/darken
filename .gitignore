# Scion runtime state — never commit
.scion/runtime/
.scion/cache/
.scion/agents/

# Container and grove state
.scion/grove-state/
.scion/worktrees/
.scion/state.yaml
.scion/server.log
.scion/server.pid

# macOS
.DS_Store

# Editor
*.swp
*.swo
.idea/
.vscode/

# Per-harness staged skills (rebuilt on each spawn from manifest).
.scion/skills-staging/

# Darwin recommendation YAML emitted post-pipeline (operator decides via `darkish apply`).
.scion/darwin-recommendations/
